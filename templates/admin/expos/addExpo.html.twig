{% extends 'base.html.twig' %}

{% block title %}Les Photos de Joël | Admin - Créer exposition
{% endblock %}

{% block body %}

	{% include "admin/adminNav.html.twig" %}

	<div class="container">
		<div class="card bg-dark">
			<div class="card-header">
				<h2>Créer/Modifier une exposition</h2>
			</div>
			<div class="card-body">
				{{ form_start(form)}}

				{{ form_row(form.title)}}
				{{ form_row(form.eventDate)}}
				{{ form_row(form.place)}}
				{{ form_row(form.path)}}
				{{ form_row(form.content)}}

				<input type="submit" value="Valider">
				{{ form_end(form)}}
			</div>
		</div>
	</div>

{% endblock %}
