{% extends 'base.html.twig' %}

{% block title %}Les Photos de Joël{% endblock %}

{% block body %}

<h1>{{ base.motPageAccueil|raw}}</h1>


{% if base.photoAccueilPath == NULL %}
<img style="width: 350px" src="{{ asset('img/default/Default.png' ) }}" alt="">
{% else %}
<figure class="text-center">
  <img src="{{ asset('img/'~ base.photoAccueilPath ) |imagine_filter('home') }}"
    alt="{{ base.titreDuSiteHeader }}">
</figure>
{% endif %}

{% endblock %}